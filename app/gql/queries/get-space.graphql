query getSpace($id: ID!) {
  getSpace(id: $id) {
    id
    avatar
    maximumPeople
    name
    openHours {
      id
      spaceId
      closeTime
      dayOfTheWeek
      openTime
    }
    organization {
      id
      name
      slug
    }
    slug
  }
}