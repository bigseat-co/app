query listSpaces {
  listSpaces {
    id
    avatar
    maximumPeople
    name
    openHours {
      id
      spaceId
      closeTime
      dayOfTheWeek
      openTime
    }
    organization {
      id
      name
      slug
    }
    slug
  }
}