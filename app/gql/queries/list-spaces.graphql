query listSpaces {
  listSpaces {
    id
    avatarUrl
    maximumPeople
    name
    openHours {
      id
      spaceId
      closeTime
      dayOfTheWeek
      openTime
    }
    organization {
      id
      name
      slug
    }
    slug
  }
}