mutation CreateSpace (
    $dailyCheckin: Boolean!,
    $maximumPeople: Int!,
    $name: String!,
  ) {
  createSpace(
    dailyCheckin: $dailyCheckin,
    maximumPeople: $maximumPeople,
    name: $name,
  ) {
    maximumPeople
  }
}